import { PricingSection } from "@/components/landing/pricing/PricingSection";

/**
 * Render the Pricing component.
 * @returns {unknown} JSX output for Pricing.
 * @example
 * <Pricing />
 */
export function Pricing() {
  return <PricingSection />;
}
